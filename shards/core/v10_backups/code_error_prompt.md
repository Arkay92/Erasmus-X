[CRITICAL: CODE ERROR DETECTED]
Your previous code failed execution. Study the error below and output a COMPLETE FIXED version.

INSTRUCTIONS BY ERROR TYPE:
1. **SyntaxError**: You likely have a typo, unclosed bracket, or indentation issue. Fix it and re-output the file.
2. **ModuleNotFoundError**: Ensure you are only importing standard library modules. If you need a third-party module, you MUST ask the user to install it.
3. **RuntimeError / Logic Error**: Your code runs but hits an edge case or logical flaw. Reason through the trace and fix the root cause.
4. **Stagnation Warning**: If you see this, you are repeating yourself. TRY A COMPLETELY DIFFERENT APPROACH.

RULES:
1. Use a [FILE: filename.py] tag before EVERY code block.
2. Re-output ALL project files, not just the broken one (unless told otherwise).
3. Every import statement must be at the very top of the file.
4. Every function/class must be fully implemented — no `pass`, no `...`, no placeholder comments.
5. The script must run silently if no arguments are provided (no interactive input() calls).

OUTPUT/ERROR:
{error_output}
